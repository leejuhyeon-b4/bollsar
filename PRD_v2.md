# 애배력 PRD v2

> **문서 상태**: v1(작품 중심) 전면 재설계 버전. v1의 리서치 결론(경쟁사 분석, OCR 실측 테스트 결과)은 그대로 유효하며, 이 문서는 데이터 모델/정보구조를 배우 중심으로 재구성한 것.
> **재설계 계기**: 개발 착수 전 코드 뼈대 단계(Supabase 미연결, 목업 데이터만 존재)에서 아키텍처 오류를 발견하여 조기 수정.

---

## 1. 서비스 개요

**애배력**은 뮤지컬 팬이 자신이 좋아하는 배우("애배")를 중심으로, 그 배우가 출연하는 모든 작품의 회차 스케줄 · 이벤트 정보 · 관극 기록/정산을 한곳에서 관리하는 서비스.

### 1.1 한 줄 정의
> 여러 작품에 흩어진 공식 정보(캐스팅, 회차 이벤트)를 배우 기준으로 통합하고, 자동화 파이프라인으로 유지비용을 낮춘 개인용 관극 관리 툴.

### 1.2 최소 성공 기준
- 운영자 본인(강홍석 팬)이 실제로 계속 쓰는 서비스가 될 것 — 사업적으로 실패해도 개인 도구로서 가치가 유지되는 구조
- 작은 규모라도 실사용자가 존재할 것

---

## 2. 차별화 근거 (경쟁 리서치 요약)

| 서비스 | 강점 | 애배력이 채우는 빈틈 |
|---|---|---|
| 뮤연 (muyeon.com) | 137개 작품, 무료, 로그인 없음 | 작품 단위 인덱스일 뿐, 배우 중심 통합 뷰 없음 |
| Loopin | 정산판 SaaS, 로그인 기반 | 회차 이벤트 정보(첫공무대인사, 커튼콜 특전 등) 다루지 않음 |
| 포도알/너겟 | 기록+후기+정산 통합, 예매내역 자동인증 | 시각적 캘린더 부재(리스트형), 페어/좌석 정산 얕음, 양도 티켓 대응 약함, 회차 이벤트 정보 없음 |
| 개인 팬메이드 사이트 (예: chichiandpopo) | 회차 이벤트 태그, 배우별 정산까지 이미 구현 | **작품 1개 = 사이트 1개** 구조라 여러 작품 통합 불가, localStorage라 기기 간 동기화 불가, 전량 수작업 추정 |

**결론**: 개별 기능 단위로는 이미 어딘가에 다 존재함. 애배력의 존재 이유는 새 기능이 아니라 **"배우 중심 통합 + 서버 동기화 + 반자동 데이터 파이프라인"**이라는 아키텍처 차이.

### 2.1 명시적으로 하지 않을 것 (Non-goals)
- 후기/평점/리뷰 기능 — 포도알이 이미 성숙하게 함, 경쟁 안 함
- 예매내역 자동 연동 — 양도 티켓이 많은 팬덤 특성상 실효성 낮음 (너겟 사례로 확인)
- 전체 작품 커버(뮤킷처럼) — 1인 운영 capacity 초과, 배우 중심 확장으로 대체

---

## 3. 핵심 아키텍처 원칙

### 3.1 데이터의 진짜 주인은 "배우"가 아니라 "회차"
- 관극 기록(내가 본 회차, 좌석)은 **(작품, 회차, 좌석)** 단위로 딱 한 번만 저장
- 배우 페이지는 이 데이터를 배우 기준으로 필터링해서 보여주는 **뷰**일 뿐
- 같은 회차에 배우 A, B가 같이 출연하면, A 페이지에서 체크한 기록이 B 페이지에도 자동 반영됨 (자동 연동)
- 삭제도 동일 원칙으로 양쪽에 전파

### 3.2 겉보기엔 배우별 개별 앱, 실제로는 단일 서비스
- URL 구조: `애배력.com/[배우slug]` (예: `/강홍석`, `/김도빈`)
- 코드/배포는 하나, 배우 slug 파라미터로 같은 템플릿이 다른 데이터를 렌더링
- 유저는 자기 배우 페이지만 홈 화면에 추가해서 "전용 앱"처럼 사용

### 3.3 배우 하나 입력 = 그 작품 전체 데이터 확보 (자동 부산물)
- 캐스팅 스케줄표는 항상 전체 배역이 한 표에 나옴 → 배우 A를 위해 입력한 캐스팅표에 배우 B~F 정보가 자동으로 같이 들어옴
- 즉 "작품 캘린더"는 별도 기능이 아니라 배우 데이터의 부산물

---

## 4. 정보구조 & 로그인 경계

| 화면 | 로그인 | 비고 |
|---|---|---|
| 배우 페이지 (`/[배우]`) — 캘린더, 이벤트 정보 조회 | ❌ 불필요 | 티켓팅/양도 상황 즉시 조회 우선 |
| 작품 캘린더 (배우 페이지 내 진입) | ❌ 불필요 | 배우 데이터의 부산물 |
| 검색하기 | ❌ 불필요 | v1과 동일 |
| 회차 체크(내가 감) + 좌석 입력 | ✅ 필요 | 개인 데이터, 기기 간 동기화 필요 |
| 정산판 (배우 페이지 내 통합 표시) | ✅ 필요 | 위 체크 데이터를 집계한 뷰 |

- "내 달력"이라는 **별도 화면/탭은 두지 않음** — 배우 페이지 캘린더에 통합 표시(체크 시 색상 변경)
- 단, 데이터 저장 단위(회차 기록 테이블)는 반드시 별도로 존재 (3.1 참조)
- Supabase RLS: 캐스팅/이벤트 테이블은 전체 공개 read, 관극 기록 테이블은 본인만 read/write

---

## 5. 데이터 모델 (Supabase 스키마 초안)

```
actors (배우)
- id, name, slug, profile_image

works (작품)
- id, title, theater_id, start_date, end_date, poster_image

theaters (극장) — 참고용, 좌석배치도는 작품 단위라 재사용 안 함
- id, name

roles (배역)
- id, work_id, role_name (예: "소크라테스")

performances (회차) — 캐스팅 스케줄표에서 추출되는 단위
- id, work_id, date, time, is_first_perf(첫공), is_no_show(공연없음)

performance_casts (회차별 배역-배우 매핑)
- id, performance_id, role_id, actor_id

performance_events (회차 이벤트) — 캐스팅과 별도 테이블, 산문형 공지에서 추출
- id, performance_id(nullable, 기간성 이벤트는 work_id+기간으로), event_type, description, photo_policy, is_verified

seat_maps (작품별 좌석배치도)
- id, work_id, layout_data(JSON: 블록/행/열 좌표), created_manually(bool)

my_records (개인 관극 기록) — RLS로 본인만 접근, "내 달력"의 실체
- id, user_id, performance_id, seat_id, checked_at

report_flags (오탈자/누락 제보)
- id, target_table, target_id, report_type, status(pending/resolved)
```

### 5.1 캐스팅 vs 이벤트 분리 이유 (실측 근거)
OCR 테스트 결과, 캐스팅표(표 형식)와 이벤트 공지(산문형)는 인식 난이도·검증 부담이 다름을 확인. 별도 테이블 + 별도 추출 파이프라인 필요.

---

## 6. 데이터 입력 파이프라인

### 6.1 자동 인식 흐름
1. 관리자 또는 제보자가 공식 홍보 이미지 업로드
2. Claude API(vision)로 이미지 → 구조화 데이터 초안 추출
   - 캐스팅표 → `performances` + `performance_casts`
   - 이벤트 공지 → `performance_events`
3. **개입 없이 즉시 게시** + "AI 인식·오류 가능" 배지 부착
4. 오류 발견 시 유저 제보(`report_flags`) → 확인되면 배지 제거

### 6.2 좌석배치도는 예외 — 반자동 불가
- 실측 결과, 번호 없는 좌석배치도는 AI 인식 불가 수준(도형 카운팅 문제)
- 번호 있는 배치도는 구조 파악까지는 가능하나 대규모(1000석+)에서 정확도 신뢰 못 함
- **좌석배치도는 작품마다 사람이 좌표를 확정하는 간이 에디터로 처리** (AI는 1차 초안 정도만 보조)
- 극장이 같아도 작품마다 무대 세팅이 달라 재사용 불가 확인됨 → 작품 단위 자산으로 관리

### 6.3 API 비용
- Claude Sonnet 기준 이미지 1장당 약 $0.01~0.03 수준, 서비스 규모에서 부담 없음 (본인 API 키로 과금, Claude.ai 무료 티어와 무관)

---

## 7. 정산판 스펙

### 7.1 집계 방식
- **페어(조합) 정산 아님** — 배우별 단순 관람 횟수만 집계 (`/N회` 형태, 시즌 전체 배정 회차 대비 관람 횟수)
- 배우별 배정 회차 수(분모)는 `performance_casts`에서 자동 계산, 별도 입력 불필요

### 7.2 좌석배치도 표시
- 기본 배치도는 등급색 없이 단색 빈 좌석
- 유저가 방문한 좌석만 색이 입혀짐
- **색상은 유저가 자유롭게 선택** (등급색 개념이 없으므로 충돌 리스크 없음)
- 방문 횟수(1회/2회/3회/4회+)에 따라 선택한 색의 진하기(명도/투명도) 자동 단계화

### 7.3 완성본 이미지 저장
- html-to-image 등으로 정산판을 하나의 이미지로 캡처 가능
- 정산판 이미지 구성 순서: **배우별 관람 횟수 → 좌석배치도 → (선택) 페어 조합**
- 페어 조합 블록은 좌석배치도 바로 아래, 독립 섹션으로 표시하며 표시 여부를 유저가 토글 (요약 이미지 사이즈 유지 목적, 기본값 숨김)
- 페어는 관극 기록 한 건마다 캐스팅 전체를 나열하는 방식이 아니라, 유저가 **배우를 최대 4명까지 골라** 그 조합을 몇 번 봤는지만 집계하는 방식 — 더블캐스팅 배역이 여러 개면 조합 수가 기하급수로 늘어나므로 전체 조합을 다 보여주지 않는다
- 이 기능 자체는 차별화 요소는 아님(누구나 구현 가능) — 정산판 장르의 필수 참가 자격으로 취급

---

## 8. 배우 확장 전략 (가지치기)

| 티어 | 범위 | 확장 방식 |
|---|---|---|
| 0. 핵심 | 강홍석, 김도빈 (초기 배우진) | 직접 입력, 최우선 유지보수 — 절대 업데이트 지연 없도록 |
| 1. 자동 부산물 | 0번 배우 출연작의 전체 공동출연 배우 | 캐스팅표 입력 시 자동 확보, 추가 작업 없음 |
| 2. 조건부 확장 | 1번 배우의 **다른** 작품 | 여력 될 때만 확장, 또는 제보/요청 누적 시 |
| 3. 제보 전용 | 그 외 전부 | "이미 나온 작품/배우만 이용해주세요" 안내 + 제보 대기 |

**원칙**: 2번 확장을 위해 0번의 업데이트 신뢰도를 희생하지 않는다. 배우 페이지 업데이트 지연이 이탈의 가장 큰 원인.

---

## 9. 커뮤니티 제보의 재정의

기존(v1) 관점: 커뮤니티 제보가 데이터 확보의 주요 수단 → **폐기**. 실측 결과 대부분의 이벤트 정보는 제작사가 이미 공식적으로 공지하고 있음이 확인됨 (v1의 "제보 의존" 전제가 틀렸음).

새 관점: 제보는 **사전 공지로 커버 안 되는 예외 상황 전용**
- 당일 대역 교체
- 사전 공지 없던 즉흥 이벤트
- OCR 오인식/누락 정정

---

## 10. 기술 스택 (변경 없음)

- Next.js, TypeScript, Tailwind CSS
- Supabase (Auth + Postgres, RLS로 개인 데이터 보호)
- Vercel
- Claude API (vision) — 이미지 → 구조화 데이터 추출
- PC-first 반응형, 다크모드 MVP 제외

---

## 11. 오픈 이슈 (다음에 정할 것)

1. 좌석배치도 간이 에디터 UI 구체 설계 (격자 자동생성 규칙)
2. `my_records`와 배우 페이지 캘린더 표시 간 실시간 연동 방식 (자동 연동 시 "다른 배우 페이지에서 체크됨" 표시 UX)
3. 배우 slug 개설 요청/제보 접수 플로우 (티어 3 → 티어 2 전환 기준)
4. 정산판 이미지 캡처 시 페어 조합 토글 UI

---

## 12. 마일스톤 (초안)

1. Supabase 스키마 구축 (5장 기준)
2. 강홍석/김도빈 캐스팅+이벤트 데이터 수동 입력 (파이프라인 검증 전이므로)
3. 배우 페이지 캘린더 뷰 구현 (로그인 없이)
4. 로그인 + 회차 체크 + 정산판 뷰 구현
5. OCR 파이프라인 연결 (6장)
6. 좌석배치도 간이 에디터 (11.1 설계 후)
