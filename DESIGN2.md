---
version: alpha
name: 애배력 색 체계
description: PRD-1st.md 7장의 색 정의를 단일 출처로 분리한 문서. 연보라 캔버스(#F4F2F7) 위에 흰 카드를 얹는 차분한 한국어 서비스 톤이며, 작품·배우를 구분하는 8색 팔레트를 화면마다 재배정해 쓴다. 색만 다루고 타이포그래피·간격·컴포넌트는 다루지 않는다.

colors:
  # ── 기반 ──
  canvas:        "#F4F2F7"   # 배경 — 연보라 회색
  surface:       "#FFFFFF"   # 카드
  ink:           "#1C1826"   # 본문 텍스트
  ink-soft:      "#6B6478"   # 보조 텍스트
  ink-faint:     "#9A93AC"   # 흐린 텍스트
  hairline:      "#E0DCE8"   # 경계선

  # ── 구분색 팔레트 (배정 순서 고정) ──
  cat-1:         "#6B3FA0"   # 보라
  cat-2:         "#B34E14"   # 주황
  cat-3:         "#16697A"   # 청록
  cat-4:         "#B02D6E"   # 자홍
  cat-5:         "#6B7028"   # 올리브
  cat-6:         "#2C5BB5"   # 파랑
  cat-7:         "#7A4A28"   # 갈색
  cat-8:         "#736A87"   # 회보라

  # ── 구분색 연한 단계 (한 배우가 두 작품에 동시 출연할 때) ──
  cat-1-soft:    "#E4DBF2"
  cat-2-soft:    "#F7E2D4"
  cat-3-soft:    "#D7E9ED"
  cat-4-soft:    "#F6DAE8"
  cat-5-soft:    "#E6E8D4"
  cat-6-soft:    "#DBE3F6"
  cat-7-soft:    "#EBDFD6"
  cat-8-soft:    "#E5E2EB"

  # ── 상태색 (원안에 없던 확장) ──
  unverified:    "#B37A14"   # 미검증 제보 배지
  unverified-bg: "#FAF0DC"
  success:       "#2F6B45"   # 검수 완료 · 반영됨
  danger:        "#A62B32"   # 캐스팅 변경 · 오류 정정
  danger-bg:     "#FAE3E4"

---


## 개요

PRD-1st.md 7장의 색 정의를 그대로 옮긴 문서다. **색만 다룬다.** 타이포그래피, 간격, 컴포넌트 형태는 이 문서의 범위가 아니다.

체계는 두 층으로 되어 있다. 아래층은 화면 전체를 지탱하는 **무채에 가까운 연보라 계열** — `{colors.canvas}` 배경 위에 `{colors.surface}` 흰 카드를 얹고, 텍스트는 `{colors.ink}`에서 `{colors.ink-faint}`까지 세 단계로 내려간다. 이 층에는 채도가 거의 없다. 위층은 **작품과 배우를 구분하는 8색 팔레트**로, 오직 데이터를 구분하기 위해서만 존재한다.

이 분리가 이 체계의 전부다. 바탕이 조용해야 구분색이 일한다. 바탕에 색을 쓰기 시작하면 달력 안의 작품 구분이 바로 무너진다.

**핵심 성질**
- 배경은 순백이 아니라 아주 옅은 보라 회색(`{colors.canvas}`)이다. 흰 카드가 배경에서 떠 보이게 하는 유일한 장치이므로 순백으로 바꾸지 않는다.
- 채도 있는 색은 **작품·배우 구분에만** 쓴다.
- 구분색은 **전역 고유색이 아니다.** 화면마다 처음부터 다시 배정한다.
- 다크 모드 없음.

## 기반색

| 용도 | 토큰 | 값 |
| --- | --- | --- |
| 배경 | `{colors.canvas}` | `#F4F2F7` |
| 카드 | `{colors.surface}` | `#FFFFFF` |
| 본문 텍스트 | `{colors.ink}` | `#1C1826` |
| 보조 텍스트 | `{colors.ink-soft}` | `#6B6478` |
| 흐린 텍스트 | `{colors.ink-faint}` | `#9A93AC` |
| 경계선 | `{colors.hairline}` | `#E0DCE8` |

`{colors.ink}`는 순흑이 아니라 보라가 섞인 먹색이다. 배경의 보라 기운과 같은 축에 있어 화면이 하나로 읽힌다.

`{colors.ink-faint}`는 흰 카드 위에서 약 2.9:1이다. **본문에 쓰지 않는다.** 비활성 상태, 입력창 플레이스홀더, 이미 지난 회차처럼 읽히지 않아도 되는 것에만 쓴다.

## 구분색 팔레트

### 배정 순서

순서를 고정한다. 항목이 n개면 앞에서 n개를 순서대로 가져다 쓴다.

| 순서 | 이름 | 값 | 연한 단계 | 흰 카드 대비 |
| --- | --- | --- | --- | --- |
| 1 | 보라 | `#6B3FA0` | `#E4DBF2` | 7.4:1 |
| 2 | 주황 | `#B34E14` | `#F7E2D4` | 5.2:1 |
| 3 | 청록 | `#16697A` | `#D7E9ED` | 6.3:1 |
| 4 | 자홍 | `#B02D6E` | `#F6DAE8` | 6.1:1 |
| 5 | 올리브 | `#6B7028` | `#E6E8D4` | 5.2:1 |
| 6 | 파랑 | `#2C5BB5` | `#DBE3F6` | 6.3:1 |
| 7 | 갈색 | `#7A4A28` | `#EBDFD6` | 7.3:1 |
| 8 | 회보라 | `#736A87` | `#E5E2EB` | 5.0:1 |

앞의 네 색이 서로 가장 멀어서, 실제로 가장 자주 발생하는 2~4개 항목 상황에서 확실히 구분된다. **파랑을 보라 바로 뒤에 두지 않은 것이 핵심이다.** 두 색은 작은 칩에서 잘 구분되지 않는다.

여덟 색 모두 흰 카드 위에서 5:1 이상이라 얇은 3px 바나 작은 글자로 써도 읽힌다. 흰 글자를 얹어도 같은 대비가 나온다. `{colors.canvas}` 배경 위에서는 수치가 조금 낮아지지만 전부 4.5:1을 넘는다.

### 연한 단계 — 농도로 작품 구분하기

한 배우가 두 작품에 동시 출연할 때 **계열은 배우, 농도는 작품**으로 나눈다(PRD 6.5).

- 진한 단계(`cat-n`) = 그 배우의 주된 작품, 또는 목록에서 먼저 나온 작품
- 연한 단계(`cat-n-soft`) = 두 번째 작품. 채움은 연한 색, 글자는 `{colors.ink}`, 테두리는 진한 단계

세 작품 이상이 겹치면 농도로 더 쪼개지 않는다. 판별이 안 된다. 그때는 색을 하나 더 배정하고 범례에 명시한다.

### 사용 규칙

- **화면마다 새로 배정한다. 전역 고유색을 두지 않는다.** 작품이 수백 개로 늘어나면 색이 모자라기 때문이다
- 구분색은 흰 카드 면 위에서만 쓴다. `{colors.canvas}` 배경에 직접 올리면 대비가 떨어진다
- 나타나는 자리는 세 곳뿐이다 — 달력 셀의 좌측 바, 필터 칩의 채움, 범례의 표식
- **버튼과 링크에는 쓰지 않는다.** 구분색이 행동 색으로도 쓰이면 "이건 작품 표시인가 누를 수 있는 것인가"가 흐려진다
- 색 규칙 범례를 달력 위에 항상 노출한다

### 색에만 의존하지 않기

여덟 색을 다 써도 색각 이상이 있는 사용자에게는 2·7번(주황·갈색), 5·8번(올리브·회보라)이 붙어 보일 수 있다. 색은 **보조 단서**로만 둔다.

- 달력 셀에는 색과 함께 작품 약칭을 적는다. 모바일에서 약칭이 빠지면 탭했을 때 상세가 열려 텍스트로 확인된다
- 필터 칩은 색뿐 아니라 선택 여부를 테두리와 점으로도 표시한다
- 범례에는 색 표식과 작품명을 항상 나란히 둔다

## 상태색

원안에 없던 확장이다. 제보와 캐스팅 변경을 표시하려면 필요하다.

| 용도 | 토큰 | 값 | 쓰는 곳 |
| --- | --- | --- | --- |
| 미검증 | `{colors.unverified}` | `#B37A14` | 제보 즉시 반영분의 배지. 배경은 `{colors.unverified-bg}` |
| 검수 완료 | `{colors.success}` | `#2F6B45` | 관리자 승인 표시 |
| 변경·정정 | `{colors.danger}` | `#A62B32` | 캐스팅 변경 이력, 오류 정정. 배경은 `{colors.danger-bg}` |

세 색 모두 구분색 팔레트와 겹치지 않게 골랐다. 특히 `{colors.danger}`는 4번 자홍과, `{colors.unverified}`는 2번 주황과 헷갈릴 수 있으므로 **항상 글자를 동반한 배지 형태로만** 쓰고 색 단독으로 의미를 전달하지 않는다.

## Do / Don't

### Do
- 배경 `{colors.canvas}` 위에 `{colors.surface}` 흰 카드를 얹는 2층 구조를 유지한다
- 채도 있는 색은 작품·배우 구분에만 배급한다
- 구분색은 배정 순서대로 앞에서부터 가져다 쓴다
- 구분색과 텍스트 라벨을 항상 같이 둔다
- 흐린 텍스트 `{colors.ink-faint}`는 읽히지 않아도 되는 것에만 쓴다

### Don't
- 배경을 순백으로 바꾸지 않는다. 카드가 배경에 묻힌다
- 작품 색을 전역으로 고정하지 않는다. 작품이 늘면 반드시 모자란다
- 구분색을 버튼·링크·내비게이션에 쓰지 않는다
- 농도 차이만으로 세 개 이상을 구분하지 않는다
- 상태색을 글자 없이 색 점 하나로만 쓰지 않는다
