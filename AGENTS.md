# AGENTS.md

## Workflow
- 쉬운 작업과 커밋/배포 준비는 가능하면 하위 모델/에이전트에 위임한다. 사용자가 모델을 직접 바꿀 필요는 없다.
- 같은 현상에 비슷한 해결을 2번 시도해도 실패하면 3번째부터 같은 방식 반복 금지. 구조/접근법 자체를 바꾸고 원인·실패한 방식·새 접근·영향 파일을 짧게 보고한다.
- 큰 파일은 전체 읽기 금지. 검색/심볼/관련 문단·라인만 읽는다. 전체 읽기가 꼭 필요할 때만 예외.
- Serena, Ponytail, Codex with ChatGPT가 사용 가능하면 항상 켜두고 적극 활용한다. 사용할 수 없으면 가능한 도구로 진행하고, 사용하지 않은 도구를 사용했다고 말하지 않는다.
- 패치 성공 ≠ 문제 해결. 실제 사용자 화면/동작까지 확인한다.
- `git add .` 금지. 필요한 파일만 명시적으로 stage한다.

## Seat map
- 좌석배치도 엔진은 작품 테마와 분리된 재사용 구조다.
- 작품 변경: 색상·폰트·배경·장식·캐스팅 UI만 변경.
- 극장 변경: 좌석/열/통로/층 데이터만 교체.
- 좌석 개별 CSS border 금지. `seat-lines.js`의 공용 SVG 선 렌더링을 사용한다.
- 공유선은 한 번만 그리고 꼭짓점은 직각으로 연결한다.
- 끝좌석 크기를 줄이거나 margin/translate/pixel 보정으로 맞추지 않는다.
- 정산판: 11×12px, SVG 0.5px.
- 확대보기: 15×15px, SVG 1px.
- Export도 동일한 SVG 원칙을 사용한다.
- 좌석 수정 후 정산판 / 확대보기 / PNG / 모바일을 모두 확인한다.
- `seat-lines.js`와 export 구조는 좌석 렌더링 문제 외에는 건드리지 않는다.
